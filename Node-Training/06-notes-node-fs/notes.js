const fs = require('fs');

var addNote = (title, body) => {
    
//add note to a file notes-data.json  

}
var getAll = () => {
//get all notes
}
var getNote = (title) => {
//get note with title passed as argument
}
var removeNote = (title) => {
//remove note with title passed as argument
}
module.exports = {
    addNote,
    getAll,
    getNote,
    removeNote
}
/* same as
module.exports = {
    addNote : addNote
}

*/